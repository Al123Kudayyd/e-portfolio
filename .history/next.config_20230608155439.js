/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['upload.wikimedia.org'],
    domains: ['movie-project-woad.vercel.app']
  },
}

module.exports = nextConfig
