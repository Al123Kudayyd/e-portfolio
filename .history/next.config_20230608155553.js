/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['upload.wikimedia.org', 'movie-project-woad.vercel.app'],
  },
}

module.exports = nextConfig
